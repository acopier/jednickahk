import localFont from 'next/font/local';

export const skautBold = localFont({
  src: './skaut-bold.woff2',
  variable: '--font-skaut-bold',
});
export const theMix = localFont({
  src: './themix-400.woff',
  variable: '--font-themix',
});
